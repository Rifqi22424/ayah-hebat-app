import 'package:flutter/material.dart';

class BookLendingListPage extends StatelessWidget {
  const BookLendingListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}