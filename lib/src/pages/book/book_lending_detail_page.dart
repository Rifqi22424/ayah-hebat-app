import 'package:flutter/material.dart';

class BookLendingDetailPage extends StatelessWidget {
  const BookLendingDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}